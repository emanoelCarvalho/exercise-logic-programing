# Imprima em ordem decrescente os numeros pares entre 100 e 0 (inclusive).

for i in range(100, -1, -1) : # Loop que vai de 100 até 0
     if i % 2 == 0 : # se o número for par
            print(i) # Imprime o numero

# Imprima em ordem crescente os numeros impares entre 0 e 100 (inclusive).